interface:
  display_name: "Pinpoint"
  short_description: "Analyze and route research requests"
  default_prompt: "Use $pinpoint to analyze this request with trend scan, anti-consensus scan, perspective expansion, and one consolidated brief, then route it to needs-confirmation, deep-research, light-answer, or skill-route. Preserve legacy top-level JSON output when routing to deep-research."
